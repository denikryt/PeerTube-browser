---
description: Read and apply Antigravity constraints mapping for the project process
---
1. Read the project policy from `.agents/rules/implementation-constraints.md`.
2. Apply the constraints and procedures defined in `.agents/rules/` and `.agents/protocols/`.
3. Keep track of status only via `dev/TASK_LIST.json`, `dev/map/DEV_MAP.json`, and `dev/FEATURE_PLANS.md`.
4. Wait for explicit execution commands matching the format defined in `.agents/rules/execution-triggers.md`.
