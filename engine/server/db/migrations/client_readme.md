# Engine Migration Resource Notes

Engine migration resources under this directory are current-shape compatibility
resources used by existing runtime `ensure_*` wrappers. They do not replace the
Client users DB migrations, crawler `schema.sql`, or historical whitelist
migration helpers.
