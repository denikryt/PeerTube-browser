# AGENTS.md

> [!IMPORTANT]
> **Project rules and protocols have been moved to `.agents/`.**

- **Hard Policies**: See `.agents/rules/` for mandatory constraints.
- **Procedures**: See `.agents/protocols/` for step-by-step instructions.
- **Workflows**: See `.agents/workflows/` for command execution details.

This file is now a redirect stub. Canonical rules are maintained in the `.agents/` directory.
